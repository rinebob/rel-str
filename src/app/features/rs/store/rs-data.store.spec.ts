import { RsDataStore } from './rs-data.store';

describe('RsDataStore', () => {
  // Signal stores are values, not classes. No instantiation needed.
  // let store: typeof RsDataStore;

  beforeEach(() => {
    // store = RsDataStore;
  });

  it('should be defined', () => {
    // expect(RsDataStore).toBeDefined();
  });
});
