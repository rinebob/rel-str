import { RsDataService } from './rs-data.service';

describe('RsDataService', () => {
  let service: RsDataService;

  beforeEach(() => {
    service = new RsDataService();
  });

  it('should be created', () => {
    // expect(service).toBeTruthy();
  });
});
